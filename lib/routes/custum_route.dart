/*import 'package:flutter/material.dart';
import 'route_names.dart';

class CustomRoute {
  static Route<dynamic> allRoutes(RouteSettings settings) {
    var r = MaterialPageRoute(builder: (_) => NotFoundPage());
    switch (settings.name) {
      case homeRoute:
        r = MaterialPageRoute(builder: (_) => HomePage());
        break;
      case aboutRoute:
        r = MaterialPageRoute(builder: (_) => AboutPage());
        break;
      case settingRoute:
        r = MaterialPageRoute(builder: (_) => SettingPage());
    }
    return r;
  }
}
*/
